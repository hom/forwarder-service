[project]
name = "forwarder-service"
version = "0.1.0"
description = "A Python API service that accepts POST /api/forwarder requests"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.46.0",
]
